data:
  name: 'simple'
  path: '/data/hit2d/processed-flav-shower-delaunay'
  t_v_split: 0.05

trainer:
  device: 6
  batch_size: 32
  n_epochs: 50
  train_name: 'delaunay1'
  summary_dir: '/scratch/GraphDUNE/summary'
  output_dir: '/scratch/GraphDUNE/params'

model:
  name: 'MultiHead'
  input_dim: 9
  hidden_dim: 64
  classes: 4
  n_iters: 4
  loss_params:
    func: 'CrossEntropyLoss'
  arrange_data: 'arrange_graph'
  arrange_truth: 'arrange_graph'

  step_size: 5
  gamma: 0.8

  activation_params:
    activation: 'mish'

  optimizer_params:
    optimizer: 'AdamW'
    AdamW:
      lr: 1.e-4
      weight_decay: 0.01
    Fromage:
      lr: 0.01
      
  metric_params:
    metrics: 'Classification'
    include_batch_metrics: False
    Classification:
      class_names: [ 'false', 'shower', 'muon', 'hadronic' ]

test:
  state_dict: 'params/delaunay1/checkpoints/model_checkpoint_MultiHead_170320_e3b7113b80_hewesje.best.pth.tar'
